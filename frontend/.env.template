# Leave empty to use Vite's /api proxy during local development.
VITE_API_BASE_URL=

# Optional. Use this when the backend is not running on localhost:8000.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
